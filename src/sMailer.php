<?php namespace Seiger\sMailer;

/**
 * Provide the package-level service entry point for sMailer capabilities.
 *
 * The 2.x foundation keeps this existing binding stable while concrete
 * subscriber, campaign, and delivery services are introduced incrementally.
 *
 * @since 1.0.0
 */
class sMailer
{
}
